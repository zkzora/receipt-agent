# Windows-friendly: flat node_modules instead of symlinks.
# Avoids node failing to resolve packages (tsc, @types/node) through pnpm's
# symlinked store when the install was created from a POSIX shell.
node-linker=hoisted
